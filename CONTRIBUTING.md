# Contributing

Use short-lived `feature/*`, `fix/*`, or `docs/*` branches. Keep `main` green. Do not reinterpret game rules: cite the controlling specification and record an implementation question when authority is insufficient or contradictory.

Before proposing a change, run `pnpm typecheck`, `pnpm lint`, `pnpm test`, and `pnpm build`.
